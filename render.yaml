services:
  - type: web
    name: yodda-premiumv3
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_complete:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0